<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>
        <variable
            name="contact"
            type="com.mrmannwood.hexlauncher.contacts.ContactData" />
        <variable
            name="adapter"
            type="com.mrmannwood.hexlauncher.contacts.ContactsDatabindingAdapter" />
    </data>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="80dp"
        android:orientation="horizontal">

        <com.mrmannwood.hexlauncher.view.HalfHexagonView
            android:id="@+id/hex_start"
            android:layout_width="30dp"
            android:layout_height="60dp"
            android:src="@color/primary_translucent"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/contact_name"
            />

        <View
            android:layout_width="wrap_content"
            android:layout_height="60dp"
            android:layout_marginEnd="40dp"
            android:layout_centerVertical="true"
            android:layout_alignStart="@+id/hex_start"
            android:layout_alignEnd="@+id/contact_image_container"
            android:layout_marginStart="30dp"
            android:background="@color/primary_translucent"/>

        <TextView
            android:id="@+id/contact_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/contact_image_container"
            android:text="@{contact.name}"
            android:textColor="@color/colorOnPrimary"
            android:textSize="18sp"
            android:textStyle="bold"
            />

        <RelativeLayout
            android:id="@+id/contact_image_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:contentDescription="@{adapter.getContactPictureContentDescription(contact)}">

            <com.mrmannwood.hexlauncher.view.HexagonView
                android:id="@+id/hex_background"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_centerInParent="true"
                android:scaleType="fitCenter"
                android:src="@color/colorSecondary"
                />

            <com.mrmannwood.hexlauncher.view.HexagonView
                android:id="@+id/contact_image"
                android:layout_width="75dp"
                android:layout_height="75dp"
                android:layout_centerInParent="true"
                android:scaleType="fitCenter"
                android:src="@{adapter.getContactImageSource(contact)}"
                />

            <TextView
                android:id="@+id/contact_letter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:visibility="@{adapter.getContactLetterVisibility(contact)}"
                android:text="@{adapter.getContactLetter(contact)}"
                android:textColor="@color/colorOnPrimary"
                android:textSize="26sp"
                android:textStyle="bold"
                />

        </RelativeLayout>

    </RelativeLayout>

</layout>